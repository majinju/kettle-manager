
## 功能迁移
```sql
-- 字典导出
select * from sys_sjgl_tyzd t
where t.yxx='1'
  and (
        ((t.dm like 'GITEA%'
             -- or t.dm like 'SYS%'
             ) and t.zdlb='SYS_COMMON_ZDLB')
        or t.zdlb  in (
        select dm from sys_sjgl_tyzd t
        where t.yxx='1'
          and ((t.dm like 'GITEA%'
                   -- or t.dm like 'SYS%'
                   ) and t.zdlb='SYS_COMMON_ZDLB')
    )
    );

-- 数据对象
select * from sys_sjgl_sjdx t
where t.yxx = '1' and (t.DXDM like 'V_XMGL_GZTJ'
                        -- or t.DXDM like 'SYS%'
    );

-- 数据字段
select zd.* from
    sys_sjgl_sjzd zd
        inner join sys_sjgl_sjdx t on t.id=zd.SJDX
where t.yxx = '1' and (t.DXDM like 'V_XMGL_GZTJ'
    -- or t.DXDM like 'SYS%'
    );

-- 相关sql
select * from SYS_SJGL_DZD t
where t.yxx = '1' and (t.dm like 'V_XMGL_GZTJ%'
                        -- or t.dm like 'SYS%'
   );

-- 权限相关
select t.* from sys_qx_qxxx t
where t.yxx = '1' and (t.dm like 'KFZFW_GXPT2_%' or t.dm like 'KFZFW_SJTJFX%' or t.dm like 'KFZFW_KWJH%'
    or t.dm like 'KFZFW_SJDZ%' or t.dm like 'KFZFW_YXJK%' or t.dm like 'KFZFW_PTGL%' or t.dm like 'KFZFW_QXGL%'
    -- or t.dm like 'SYS%'
    );


-- 表在Navicat中选中导出表和结构即可。
-- 视图需要逐个导出。
```

## oracle迁移到mysql
### 方案1
1. 采用navicat的“工具”=》“数据传输”，选项中设置“错误继续”。
1. 同步完成后，拿出同步日志，对自动处理异常的sql语句进行处理执行。
1. 在目标数据库右键“转储sql文件”仅结构。

### 方案2
1. 在Navicat中在源库右键“转储sql文件”仅结构。
2. LOGGING([\S\s\n]*);=》;
3. DROP.*;=>空
4. mysql不支持这个语法：COMMENT .*;=>空
5. 取消用户名："SJSJ3".=》空
6. "=》空
7. VARCHAR2=》VARCHAR
8. BYTE=》空
9. NUMBER=》bigint
10. sys_guid()=》(REPLACE(UUID(),'-',''))
11. to_char(sysdate,'yyyymmddhh24miss')=》(date_format(now(), '%Y%m%d%H%i%S'))
12. clob=》text
13. " blob"=》" longblob"
14. VARCHAR(4000),=》text,（因为mysql一行有长度限制，这里将4000长度的没有默认值的改为text，因为text不允许有默认值）
15. SQL=》`SQL`（关键字要加引号，手动替换，很容易误伤）
16. text.*DEFAULT.*, => text,

结构同步后，进行数据同步：
1. 采用navicat的“工具”=》“数据传输”，选项中设置“错误继续”，不进行结构更改。


### sql语法切换
1. 时间：to_char(sysdate,'yyyymmddhh24miss')=》date_format(now(), '%Y%m%d%H%I%S')
2. 字符串拼接：ms||jp||qp=》CONCAT_WS('',ms,jp,qp)
3. mysql要求所有查询（）括号表达式时必须加别名

## 升级记录

### 2023-10-19
```sql
ALTER TABLE `sys_qx_yhxx` 
ADD COLUMN `sqm` varchar(255) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL 
    COMMENT '授权码;用于特殊场景自动登录，简化登录，但安全性稍低，需要设置为长随机字符串' AFTER `qqbfxz`;
```

### 2023-4-23
```
ALTER TABLE `sys_log_fwzr`
ADD COLUMN CDLJ varchar(2048) NULL COMMENT '菜单路径' AFTER `FHXX`,
ADD COLUMN CZLXMC varchar(255) NULL COMMENT '操作类型名称' AFTER `CDLJ`;
```

### 2022-11-29
```sql
ALTER TABLE  `sys_sjgl_file`
ADD COLUMN `parent` varchar(512) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL COMMENT '父路径' AFTER `cjrdwmc`;
update sys_sjgl_file t set t.sclj = replace(replace(t.sclj,'''',''),'select nr wj from sys_sjgl_blob where id=','') where t.sclj like 'select%';
update sys_sjgl_file t set t.parent = replace(replace(t.sclj,'/F:',''),'/benma666/upload','') where t.sclj like '/%';
update sys_sjgl_file t set t.parent = substr(t.parent,1,length(t.parent)-locate('/',reverse(t.parent))) where t.sclj like '/%';
update sys_sjgl_blob t1 set t1.id = (
select t.id from sys_sjgl_file t where t.sclj<>t.id and t.sjzt='default' and t.parent is null and t.sclj=t1.id
) where t1.id in (
select t.sclj from sys_sjgl_file t where t.sclj<>t.id and t.sjzt='default' and t.parent is null
);
```

### 2022-11-25
```sql
ALTER TABLE `sys_sjgl_grbj`
ADD COLUMN `dm` varchar(512) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL COMMENT '代码;自动根据id生成，便于形成上下级' AFTER `bjq`,
ADD COLUMN `pl` varchar(8) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL COMMENT '评论@SYS_COMMON_LJPD' AFTER `dm`,
ADD COLUMN `zan` varchar(8) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL COMMENT '赞@SYS_COMMON_LJPD' AFTER `pl`,
ADD COLUMN `cai` varchar(8) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL COMMENT '踩@SYS_COMMON_LJPD' AFTER `zan`;
ALTER TABLE `sjsj2_dev`.`sys_sjgl_grbj`
MODIFY COLUMN `pl` varchar(8) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT '1' COMMENT '评论@SYS_COMMON_LJPD' AFTER `dm`,
MODIFY COLUMN `zan` varchar(8) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT '1' COMMENT '赞@SYS_COMMON_LJPD' AFTER `pl`,
MODIFY COLUMN `cai` varchar(8) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT '1' COMMENT '踩@SYS_COMMON_LJPD' AFTER `zan`;
update sys_sjgl_grbj t set t.zan='1',t.pl='1',t.cai='1';
update sys_sjgl_grbj t set t.dm=t.id where t.sjbj is null;
update sys_sjgl_grbj t set t.dm=CONCAT_WS('',(select dm from (select * from sys_sjgl_grbj) t1 where t1.id=t.sjbj),'_',t.id) where t.sjbj is not null;
update sys_sjgl_grbj t set t.dm=CONCAT_WS('',(select dm from (select * from sys_sjgl_grbj) t1 where t1.id=t.sjbj),'_',t.id) where t.sjbj is not null;
update sys_sjgl_grbj t set t.dm=CONCAT_WS('',(select dm from (select * from sys_sjgl_grbj) t1 where t1.id=t.sjbj),'_',t.id) where t.sjbj is not null;
update sys_sjgl_grbj t set t.dm=CONCAT_WS('',(select dm from (select * from sys_sjgl_grbj) t1 where t1.id=t.sjbj),'_',t.id) where t.sjbj is not null;
update sys_sjgl_grbj t set t.dm=CONCAT_WS('',(select dm from (select * from sys_sjgl_grbj) t1 where t1.id=t.sjbj),'_',t.id) where t.sjbj is not null;
```

### 2022-11-18
```sql
CREATE TABLE `sys_wdgl_pl` (
  `ID` varchar(32) CHARACTER SET utf8 COLLATE utf8_general_ci NOT NULL DEFAULT (replace(uuid(),_utf8mb4'-',_utf8mb4'')) COMMENT '主键',
  `YXX` varchar(10) CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT '1' COMMENT '有效性@SYS_COMMON_LJPD',
  `CJSJ` varchar(50) CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT (date_format(now(),_utf8mb4'%Y%m%d%H%i%S')) COMMENT '创建时间',
  `GXSJ` varchar(50) CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT (date_format(now(),_utf8mb4'%Y%m%d%H%i%S')) COMMENT '更新时间',
  `CJRXM` varchar(32) CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT NULL COMMENT '创建人姓名',
  `CJRDM` varchar(32) CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT NULL COMMENT '创建人代码',
  `CJRDWMC` varchar(255) CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT NULL COMMENT '创建人单位名称',
  `CJRDWDM` varchar(255) CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT NULL COMMENT '创建人单位代码',
  `PX` bigint DEFAULT '9999' COMMENT '排序',
  `KZXX` varchar(4000) CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT NULL COMMENT '扩展信息',
  `XGWD` varchar(32) DEFAULT NULL COMMENT '相关文档',
  `XGPL` varchar(32) DEFAULT NULL COMMENT '相关评论',
  `CZLB` varchar(32) CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT NULL COMMENT '操作类别@SYS_WDGL_PL_CZLB',
  `NR` varchar(4000) DEFAULT NULL COMMENT '内容',
  PRIMARY KEY (`ID`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb3 COMMENT='系统-文档管理-评论';
ALTER TABLE `sys_wdgl_pl`
ADD COLUMN `bjq` varchar(8) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL COMMENT '编辑器@SYS_WDGL_WD_BJQ' AFTER `NR`;
ALTER TABLE `sys_sjgl_grbj`
ADD COLUMN `bjq` varchar(8) NULL COMMENT '编辑器@SYS_WDGL_WD_BJQ' AFTER `kzxx`;
```

### 2022-3-13

### 菜单到前端页面的映射逻辑，考虑在权限中新增字段数据对象，权限对应对象时直接选择，前端页面路由对应地址的值。
```sql
--先在权限表新增sjdx字段：SJDX	VARCHAR2(32)	Y			数据对象@SYS_SJGL_SJDX
--刷新权限管理对象，配置到打开方式后面
--批量更新新增的字段值
update sys_qx_qxxx t set t.sjdx=(select id from sys_sjgl_sjdx dx where dx.dxdm=t.dz and dx.yxx='1') where t.dzlx in ('04','03') and t.dz is not null;
--更新权限的地址为前端路由，暂时让相关开发自己添加
```
